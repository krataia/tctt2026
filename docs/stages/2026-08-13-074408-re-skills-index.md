# Stage: re-skills-index

- Started: 2026-08-13 07:44:08
- Updated: 2026-08-13 07:57:41
- Status: completed

## Task

Operator request (faithful summary, Thai → EN): "Search for reverse-engineering skills for .exe, .apk, and other formats that exist in this catalogue/listing."

Deliverables (all met):
1. Exhaustive sweep of the workspace's RE skill inventory with per-skill format coverage.
2. Consolidated, source-locked RE skill index at `docs/knowledge/reverse-engineering-skills-index.md`.
3. Mandatory stage record + memory pointer.

## Attempts and experiments

### Scope scan
- Enumerated all SKILL.md files workspace-wide. RE keyword hit counts: reverse-skill 184, vendor/claude-red 49, vendor/wshobson-agents 117 (mostly non-RE plugins), vendor/p4nda0s-reverse-skills 10, vendor/android-reverse-engineering-skill 8.
- RE-relevant sources: reverse-skill/skills specialists, reverse-skill/CTF-Sandbox-Orchestrator competition routes, vendor/p4nda0s-reverse-skills, vendor/wshobson-agents/plugins/reverse-engineering, vendor/claude-red (exploit-dev/mobile/iot/infrastructure), vendor/android-reverse-engineering-skill, workspace routing layer.

### Sweep workflow (`wf_5b74d02b-d34`, completed)
- 7 agents, 0 errors: 6 parallel source sweeps (reverse-skill specialists, competition routes, p4nda0s, wshobson RE plugin, claude-red+android+cloudflare, integration/routing) + 1 high-effort verification pass.
- Verification: all **87 reported skill paths exist** (invalid_paths=[]), **no RE skill missed** (missed_skills=[] — 86 keyword-matched files inspected and correctly excluded), all **13 target formats covered by 2+ skills** (status=covered).
- ~481k tokens, 213 tool calls, ~11 min.

### Index build
- Wrote `docs/knowledge/reverse-engineering-skills-index.md`: quick format→best-skill map (13 formats incl. .exe/PE32, .apk/DEX, ELF, .NET, iOS/Mach-O, macOS, .so, firmware, Unity IL2CPP, JS/WASM, memory dump, protocol/pcap, zip) + full catalogue by 7 source areas + routing layer + gaps.
- Added a "Reverse-engineering skill index" pointer section to `SKILLS.md`.

## Observations

- **.exe/PE32** and **.apk/DEX** are the most-covered formats (12 and 7 dedicated skills respectively), with dedicated toolkits (ida/radare2/dotnet for PE; apk-reverse/mobile-reverse/rev-dex-dumper/SimoneAvogadro for APK).
- Notable specialists: `dotnet-reverse` (.NET), `rev-u3d-dump` (Unity IL2CPP), `rev-dex-dumper` (live DEX dump), `firmware-pentest` (FSTM firmware), `macos-reverse`+`rev-ios-dump` (Apple), `js-reverse`+`dsl-vm-reverse` (JS/WASM), wshobson `memory-forensics` (Volatility).
- Routing model: primary = `ctf-sandbox-orchestrator` → narrowest `competition-*` child + at most ONE vendored supplement (exact mapping captured).
- Gaps recorded: host missing jadx/apktool/adb/frida/gdb/analyzeHeadless/bkcrack; p4nda0s unlicensed; `rev-ios-dump` uses lowercase `skill.md`; `rev-struct`/`rev-symbol` need IDA MCP; reverse-skill scripts are Windows-PowerShell-oriented.

## Lessons learned

- **Keyword grep over-approximates**: a verification pass that re-scans and *inspects* keyword-matched files is what separates genuine RE skills from false positives (e.g. PROCESSENTRY32 "pe32", HackRF radio firmware, Python `reverse=True`).
- **Path validation must be a first-class step**: 87 reported paths were all confirmed to exist by the verify agent; several sweeps initially referenced a non-existent `plugins/security-scanning/` path that was corrected to the real location.
- **A format→skill quick map is the high-value artifact**: users asking ".exe/.apk and others" want the fastest route to the right skill, not just the full catalogue; the index leads with that map.

## Outcome and next steps

- Completed: RE skills index at `docs/knowledge/reverse-engineering-skills-index.md` (87 verified skill paths, 13 formats), pointer added to `SKILLS.md`, memory saved.
- Next steps (optional): run `bootstrap-reverse.sh` (with approval) to fill the missing host toolchain (jadx/apktool/frida/gdb/analyzeHeadless/bkcrack); regenerate `.tctt-tool-index.md` after installs; optionally wire the RE index into the orchestrator's ctf-routing references.
